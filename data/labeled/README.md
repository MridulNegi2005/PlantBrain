# data/labeled

Gold-label files for evaluation, created in Interval 1:
- `entities_gold.json` — expected entity extractions (asset tags, components, failure modes) per document.
- `qa_gold.json` — the ~20 benchmark questions (including multi-hop ones) with expected cited answers.
- `compliance_gold.json` — expected compliance gap findings per asset.
